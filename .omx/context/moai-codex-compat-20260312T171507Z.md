# Ralph Context Snapshot

- Task statement: Reimplement a MoAI-ADK compatible experience for Codex, proceed with Ralph, commit and push milestones step-by-step, and finish with a README commit/push.
- Desired outcome: A usable `moai` compatibility CLI and project scaffold that works in this environment, preserves `.moai/` project contracts, and exposes MoAI-style commands backed by Codex/OMX-compatible behavior.
- Known facts/evidence:
  - Upstream `moai-adk` is Go-based but this environment does not have `go`.
  - `node` and `python3` are available locally.
  - Remote `origin` points to `https://github.com/Epsilondelta-ai/moai-adk-codex.git` and currently has no heads.
  - Existing plan artifact: `.omx/plans/moai-codex-compat-plan.md`.
  - This workspace is effectively empty aside from `.git` and `.omx`.
- Constraints:
  - Use Ralph.
  - Commit and push in stages.
  - No new dependencies without explicit request.
  - Preserve MoAI outer contract where practical.
- Unknowns/open questions:
  - Exact preferred runtime mapping for `cc`, `cg`, `glm` beyond documented compatibility intent.
  - Whether future iterations should port deeper workflow semantics beyond the initial compatibility foundation.
- Likely codebase touchpoints:
  - `package.json`
  - `bin/moai.js`
  - `src/cli/*`
  - `src/core/*`
  - `templates/*`
  - `README.md`
